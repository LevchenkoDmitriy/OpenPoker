public class CheckWinner {
    /*
    public String[] checkWin(){
        //Создаём массив победителей. Раздача кэша будет Bank/length winners
        String winners[];

        return winners;
    }
*/
    //TODO
    //Передаём параметром 2 карты игрока и 5 карт на столе. Если победитель, то записываем его в winners
    private boolean isRoyalFlush(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }

    private boolean isStraightFlush(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }

    private boolean isFourOfaKind(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }

    private boolean isFullHouse(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }

    private boolean isStraight(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }

    private boolean isThreeOfaKind(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }

    private boolean isTwoPairs(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }

    private boolean isPair(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }

    private boolean isHighCard(CardDeck.Card[] userCards, CardDeck.Card[] tableCards){
        return true;
    }
}
